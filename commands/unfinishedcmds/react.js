const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
//     //if(message.author.id != "317098359116529666") return message.reply("Sorry, you cant use that!");
//     let emoji = message.guild.emojis.find('name', );
//     message.react(emoji);
//
}

module.exports.help = {
  name: "react"
}
